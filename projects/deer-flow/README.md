# deer-flow

This subproject contains:

- source/: a snapshot of the DeerFlow project source tree (sanitized for local-only artifacts)
- learning/: architecture learning documents and reimplementation notes

## Notes

- learning/UPSTREAM_BASELINE.yaml is the single file to update when syncing upstream baseline.
- Local-only files such as .git, logs, virtualenvs, and local config are excluded from source/.
